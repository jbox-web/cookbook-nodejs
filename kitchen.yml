---
driver:
  name: docker
  use_sudo: false

provisioner:
  name: chef_solo

verifier:
  name: inspec
  reporter:
    - progress

platforms:
  - name: debian-stretch

suites:
  - name: default
    run_list:
      - recipe[nodejs]
